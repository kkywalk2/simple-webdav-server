services:
  webdav:
    build: .
    ports:
      - "8080:8080"
    environment:
      WEBDAV_ROOT: /data/webdav
      WEBDAV_DB_PATH: /data/webdav.db
    volumes:
      - webdav-data:/data

volumes:
  webdav-data:
